{
  "name": "f1-dashboard-main",
  "version": "1.0.0",
  "description": "F1 Dashboard Application with role-based access control",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "setup": "./setup.sh",
    "client": "cd client && npm start",
    "server": "cd server && npm run dev",
    "install-all": "cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["f1", "dashboard", "react", "express", "postgresql"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}